# Server
PORT=3000

# Authentication (set AUTH_ENABLED=true to require login)
AUTH_ENABLED=false
AUTH_USER=admin
AUTH_PASS=changeme

# TLS (provide paths to cert and key for HTTPS)
TLS_ENABLED=false
TLS_CERT=
TLS_KEY=

